import FormLogin from "@/components/login/FormLogin";

const page = async () => {
  return (
    <>
      <section className="section container">
        <FormLogin />
      </section>
    </>
  );
};

export default page;
